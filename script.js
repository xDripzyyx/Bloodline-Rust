const CONFIG={discord:'https://discord.gg/uJvjg8PWAp',store:'https://bloodlinerust.tebex.store',wipeDate:'2026-08-20T18:00:00',ips:['135.148.15.176:20002','135.148.172.229:28560']};
const toastEl=document.getElementById('toast');
function toast(m){toastEl.textContent=m;toastEl.classList.add('show');clearTimeout(window.__toast);window.__toast=setTimeout(()=>toastEl.classList.remove('show'),1800)}
document.querySelectorAll('[data-ip]').forEach(btn=>btn.addEventListener('click',async()=>{const ip=btn.dataset.ip;try{await navigator.clipboard.writeText(ip);toast('COPIED '+ip)}catch{toast('IP: '+ip)}}));
document.querySelectorAll('.connect').forEach(btn=>btn.addEventListener('click',()=>{window.location.href='steam://connect/'+btn.dataset.ip}));
function tick(){const distance=new Date(CONFIG.wipeDate).getTime()-Date.now();const d=Math.max(0,distance);document.getElementById('d').textContent=String(Math.floor(d/864e5)).padStart(2,'0');document.getElementById('h').textContent=String(Math.floor(d/36e5)%24).padStart(2,'0');document.getElementById('m').textContent=String(Math.floor(d/6e4)%60).padStart(2,'0');document.getElementById('s').textContent=String(Math.floor(d/1e3)%60).padStart(2,'0');document.getElementById('wipeText').textContent=distance>0?'Next wipe: '+new Date(CONFIG.wipeDate).toLocaleString():'WIPE IS LIVE — GOOD LUCK'}tick();setInterval(tick,1000);
const targets=document.querySelectorAll('.section,.wipe,.final,.server-card,.feature,.rule-grid>div,.faq-layout,.gallery-grid img');targets.forEach(e=>{e.style.opacity='0';e.style.transform='translateY(20px)';e.style.transition='opacity .7s ease,transform .7s ease'});if('IntersectionObserver' in window){const o=new IntersectionObserver(es=>es.forEach(e=>{if(e.isIntersecting){e.target.style.opacity='1';e.target.style.transform='none';o.unobserve(e.target)}}),{threshold:.08});targets.forEach(e=>o.observe(e))}else targets.forEach(e=>{e.style.opacity='1';e.style.transform='none'});
